/**
 * 
 */
/**
 * This package contains all classes related to the more specific case of 
 * finding the best path in a given grid. Thus, has the extension of {@link simulator.EventSimulator},
 *  {@link pathfinder.MainSimulator}, the XML parser that reads XML file and gets all constants
 *  needed for the simulation, {@link pathfinder.ReadFile}, and the map itself and its constituents.<p>
 *  It also contains subclasses of {@link simulator.Event} that implement its central method.<p>
 *  Lastly, contains {@link pathfinder.Individual} that contains all information that
 *  represent each individual and all methods that it requires to move and update.
 *
 */
package pathfinder;